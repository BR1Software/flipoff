# TODO
1. update the readme
2. add the option to change the number of rows and columns on launch (should affect the grid entry and layout etc.)
3. Allow counters to be used. Countdoan, stopwatch and time options (in the corner).
4. Add preprogrammed text to be added with a time to display and cylce through them.
5. Add colours
6. Add start tile on update so the tiles can update in a wave from the bottom or top or corners?
7. Add font options - News Gothic Bold is the original
8. Add a way to change all these options from the HTML (not hard coded).


To test
```bash
# Or serve locally:
python3 -m http.server 8080
# Then open http://localhost:8080
```
